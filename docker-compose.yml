version: '3.8'

services:
  bot:
    build: .
    container_name: telegram-chatwoot-bot
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
